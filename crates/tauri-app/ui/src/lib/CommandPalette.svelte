<!--
  CommandPalette — now delegates entirely to design-dojo's CommandPalette overlay.
  This file exists only to bridge the store-based open state and plugin commands.
-->
<script>
  // This component is no longer needed — CommandPalette is now rendered directly in App.svelte
  // using the design-dojo CommandPalette component. This file is kept as a no-op for
  // backward compatibility if anything still imports it.
</script>
