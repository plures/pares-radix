# validation_utils.px — Validation procedures for compose/resolver integration tests

procedure validate_input:
  trigger: manual
  check_not_empty {value: data} -> $valid
  when $valid == false:
    emit {type: "validation_failed", reason: "empty_input"}
  end

procedure sanitize:
  trigger: manual
  strip_html {text: input} -> $output
