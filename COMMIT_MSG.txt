refactor: migrate ALL procedures to dataflow syntax (no more triggers)

Complete migration of all 17 procedure files from trigger-based to
dataflow syntax. Zero trigger: keywords remain in praxis/procedures/.

Changes:
- All procedures now use typed signatures with explicit params
- Event-triggered procedures use from-queue bindings
- Output procedures use into-queue bindings
- Manual/cron procedures use explicit params (no queue bindings)
- Deleted *_dataflow.px files (superseded by main files)
- model_client field now uses StdRwLock for late binding
- set_model_client() enables post-construction model attachment

Stats: -287 net lines (639 deleted, 352 added)
  Trigger boilerplate eliminated across all procedure files.

The full .px procedure corpus is now dataflow-native:
  inbound -> preprocess -> classify -> route -> invoke -> deliver

All procedures are pure functions. No triggers. No priority ordering.
Data flows through queues. The system stops when queues are empty.
