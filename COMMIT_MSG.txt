feat: dataflow bridge — connect cerebellum to queue-driven procedures

Wires the new pluresdb-px dataflow module into pares-radix:

- DataflowBridge struct: replaces px_bridge.route_event() for new-style
  procedures. Pushes event data into graph input queues, runs to
  quiescence, reads results from output queues.

- Re-exports from pares-radix-praxis: DataflowGraph types,
  AsyncActionHandler, execute_async_with_vars — downstream crates
  access the full dataflow API through praxis.

- Updated pluresdb-px pin to 483af63 (includes dataflow module + grammar).

- Registered dataflow_bridge module in cerebellum.

Integration path: the cerebellum will gradually migrate procedures
from trigger-based (px_bridge) to queue-driven (dataflow_bridge).
Both paths coexist during migration.
